const counters = document.querySelectorAll(".stat+h1");
const container = document.querySelector(".comp-stat-container");

let activated = false;


const updateCount = () =>{
    const target = parseInt(counter.dataset.count);

    if ()
}

window.addEventListener("scoll",()=>{
    if (scrollY > container - container.scrollHeight -200 && activated == false){
        counters.forEach(counters => {
            counters.innerText = 0;
            let count = 0

        })

    }

})
